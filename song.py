from word import Word

class Song:
    def __init__(self, raw_tab):
        lyrics = self.generate_tab(raw_tab)
        if words != None:
            self.valid = True
            self.lyrics = lyrics

    #given the raw text input, generate the list of words
    #regex TODO
    def generate_tab(tab):
        return tab
